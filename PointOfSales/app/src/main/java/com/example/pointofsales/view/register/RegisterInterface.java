package com.example.pointofsales.view.register;

/**
 * RegisterInterface for callback to check if the Username (email) provided is valid
 */
public interface RegisterInterface {
    void isUsernameValid(boolean isValid);
}
